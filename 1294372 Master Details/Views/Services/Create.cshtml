@model _1294372_Master_Details.Models.Entity.Service

@{
    ViewData["Title"] = "Create Service";
}

<style>
    .form-card {
        max-width: 520px;
        margin: auto;
        border-radius: 24px;
        background: rgba(255,255,255,0.9);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(0,0,0,0.05);
        box-shadow: 0 20px 45px rgba(0,0,0,0.08);
        overflow: hidden;
    }

    .form-header {
        background: linear-gradient(135deg, #0d6efd, #4dabf7);
        color: #fff;
        padding: 30px;
        text-align: center;
    }

        .form-header h3 {
            font-weight: 800;
            margin-bottom: 5px;
        }

    .form-body {
        padding: 30px;
    }

    .form-control {
        border-radius: 14px;
        padding: 12px 14px;
        font-weight: 500;
    }

    .btn-soft {
        border-radius: 999px;
        font-weight: 600;
        padding: 10px 26px;
    }
</style>

<div class="container py-4">

    <div class="form-card">

        <!-- Header -->
        <div class="form-header">
            <div style="font-size:38px;">🛠</div>
            <h3>Create New Service</h3>
            <p class="mb-0 opacity-75">
                Add a new repair service to the system
            </p>
        </div>

        <!-- Form -->
        <div class="form-body">
            <form asp-action="Create" method="post">

                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <div class="mb-4">
                    <label asp-for="ServiceName" class="form-label fw-semibold"></label>
                    <input asp-for="ServiceName" class="form-control"
                           placeholder="e.g. Screen Replacement" />
                    <span asp-validation-for="ServiceName" class="text-danger small"></span>
                </div>

                <div class="d-flex justify-content-between align-items-center mt-4">
                    <a asp-action="Index" class="btn btn-outline-secondary btn-soft">
                        ← Back
                    </a>

                    <button type="submit" class="btn btn-primary btn-soft shadow-sm">
                        ✔ Create Service
                    </button>
                </div>

            </form>
        </div>

    </div>

</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}